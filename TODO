- change post/updateMessage to accept an instance of Configuration
- support switching the positions of time stamp and message type string
- allow toggling timestamps for animated messages (if timestamp is on the left, pad output to align it with other messages)
- implement the multi-colored effect in InProgress
